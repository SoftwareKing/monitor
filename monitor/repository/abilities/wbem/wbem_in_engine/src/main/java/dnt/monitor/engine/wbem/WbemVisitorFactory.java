package dnt.monitor.engine.wbem;

import dnt.monitor.engine.service.VisitorFactory;
import net.happyonroad.credential.CredentialProperties;

/**
 * <h1>Webem Visitor Factory</h1>
 *
 * @author Jay Xiong
 */
public interface WbemVisitorFactory extends VisitorFactory<CredentialProperties,WbemVisitor>{
}
