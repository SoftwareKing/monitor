include /lib/mixins/form

#topoLayout
  +base_form("newNodeForm")(ng-submit="create()", ng-submitted="submitted")
    .modal-header
      //button.bootbox-close-button.close(type='button', ng-click="cancel()") ×
      h3.modal-title 新增{{title}}
    .modal-body
      div(ui-view='changeContent')
    .modal-footer
      +action_submit("创建")
      +action_cancel("取消")(ng-click="cancel()")