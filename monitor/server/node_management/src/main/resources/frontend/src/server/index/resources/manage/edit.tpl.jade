include /lib/mixins/form

#topoLayout
  +base_form("editNodeForm")(ng-submit="update()", ng-submitted="submitted")
    .modal-header
      //button.bootbox-close-button.close(type='button', ng-click="cancel()") ×
      h3.modal-title 编辑{{title}}
    .modal-body
      div(ui-view='changeContent')
    .modal-footer
      +action_submit("更新")
      +action_cancel("取消")(ng-click="cancel()")